<!-- Header -->
<div class="text-center mb-12">
  <h1 class="text-5xl font-bold text-gray-900 mb-3">ActiveDataFlow Demo</h1>
  <p class="text-xl text-gray-600">Rails 8 Product Catalog Synchronization Example</p>
</div>

<!-- Stats Cards -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
  <div class="bg-white rounded-lg shadow-sm p-8 text-center border border-gray-200">
    <div class="text-5xl font-bold text-blue-600 mb-2"><%= @product_count %></div>
    <div class="text-gray-600 font-medium text-lg">Total Products</div>
    <div class="text-sm text-gray-500 mt-1">(<%= @active_product_count %> active)</div>
  </div>
  <div class="bg-white rounded-lg shadow-sm p-8 text-center border border-gray-200">
    <div class="text-5xl font-bold text-green-600 mb-2"><%= @export_count %></div>
    <div class="text-gray-600 font-medium text-lg">Exported Products</div>
    <div class="text-sm text-gray-500 mt-1">Transformed records</div>
  </div>
  <div class="bg-white rounded-lg shadow-sm p-8 text-center border border-gray-200">
    <div class="text-5xl font-bold text-purple-600 mb-2">1</div>
    <div class="text-gray-600 font-medium text-lg">Active DataFlow</div>
    <div class="text-sm text-gray-500 mt-1">ProductSyncFlow</div>
  </div>
</div>

<!-- Main Content -->
<div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-10">
  <!-- Products Section -->
  <div class="bg-white rounded-lg shadow-sm p-8 border border-gray-200">
    <h2 class="text-3xl font-bold text-blue-600 mb-4">📦 Products</h2>
    <p class="text-gray-600 leading-relaxed mb-4">The source table containing your product catalog. Each product has:</p>
    <ul class="space-y-2 mb-6">
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Name & SKU:</span> Product identification</li>
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Price:</span> Stored as decimal (e.g., $29.99)</li>
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Category:</span> Product classification</li>
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Active Flag:</span> Only active products are synced</li>
    </ul>
    <%= link_to "View Products →", products_path, class: button_classes(:primary) %>
  </div>

  <!-- Product Exports Section -->
  <div class="bg-white rounded-lg shadow-sm p-8 border border-gray-200">
    <h2 class="text-3xl font-bold text-green-600 mb-4">📄 Product Exports</h2>
    <p class="text-gray-600 leading-relaxed mb-4">The destination table with transformed product data for external systems:</p>
    <ul class="space-y-2 mb-6">
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Product ID:</span> Reference to source product</li>
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Price in Cents:</span> Converted from decimal (2999)</li>
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Category Slug:</span> URL-friendly format</li>
      <li class="text-gray-600"><span class="font-semibold text-gray-900">Export Timestamp:</span> When sync occurred</li>
    </ul>
    <%= link_to "View Exports →", product_exports_path, class: button_classes(:success) %>
  </div>
</div>

<!-- DataFlow Section -->
<div class="bg-white rounded-lg shadow-sm p-8 border border-gray-200 mb-10">
  <h2 class="text-3xl font-bold text-purple-600 mb-4">⚡ ProductSyncFlow DataFlow</h2>
  <p class="text-lg text-gray-600 leading-relaxed mb-6">
    The ProductSyncFlow demonstrates ActiveDataFlow's data transformation capabilities. 
    It automatically syncs active products to the export table with the following transformations:
  </p>

  <!-- Flow Diagram -->
  <div class="bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-8 mb-6">
    <div class="flex flex-col md:flex-row items-center justify-center gap-4 md:gap-8">
      <!-- Source Box -->
      <div class="bg-blue-600 text-white rounded-lg p-6 text-center min-w-[200px] shadow-lg">
        <div class="text-lg font-bold mb-2">Source</div>
        <div class="text-sm opacity-90">Products Table</div>
        <div class="text-xs opacity-75 mt-2">Active products only</div>
      </div>
      
      <!-- Arrow -->
      <div class="text-gray-400 text-3xl transform md:transform-none rotate-90 md:rotate-0">
        →
      </div>
      
      <!-- Transform Box -->
      <div class="bg-purple-600 text-white rounded-lg p-6 text-center min-w-[200px] shadow-lg">
        <div class="text-lg font-bold mb-2">Transform</div>
        <div class="text-sm opacity-90">Data Processing</div>
        <div class="text-xs opacity-75 mt-2">Price, category, timestamp</div>
      </div>
      
      <!-- Arrow -->
      <div class="text-gray-400 text-3xl transform md:transform-none rotate-90 md:rotate-0">
        →
      </div>
      
      <!-- Sink Box -->
      <div class="bg-green-600 text-white rounded-lg p-6 text-center min-w-[200px] shadow-lg">
        <div class="text-lg font-bold mb-2">Sink</div>
        <div class="text-sm opacity-90">Product Exports</div>
        <div class="text-xs opacity-75 mt-2">Transformed records</div>
      </div>
    </div>
  </div>

  <!-- Transformations -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-6">
    <div class="border-l-4 border-purple-600 pl-4">
      <div class="font-bold text-gray-900 mb-1">Price Conversion</div>
      <div class="text-gray-600 text-sm">$29.99 → 2999 cents</div>
      <div class="text-gray-500 text-xs mt-1">Multiply by 100 for integer storage</div>
    </div>
    <div class="border-l-4 border-purple-600 pl-4">
      <div class="font-bold text-gray-900 mb-1">Category Slugification</div>
      <div class="text-gray-600 text-sm">"Home & Kitchen" → "home-kitchen"</div>
      <div class="text-gray-500 text-xs mt-1">URL-friendly format</div>
    </div>
    <div class="border-l-4 border-purple-600 pl-4">
      <div class="font-bold text-gray-900 mb-1">Timestamp Addition</div>
      <div class="text-gray-600 text-sm">exported_at: <%= Time.current.strftime("%Y-%m-%d %H:%M") %></div>
      <div class="text-gray-500 text-xs mt-1">Track when sync occurred</div>
    </div>
  </div>

  <!-- Code Preview -->
  <div class="bg-gray-900 rounded-lg overflow-hidden mb-6">
    <div class="px-6 py-4 bg-gray-800 border-b border-gray-700">
      <span class="text-gray-400 text-sm font-mono">ProductSyncFlow</span>
    </div>
    <div class="p-6 overflow-x-auto">
      <pre class="text-sm text-gray-100 font-mono leading-relaxed"><code>class ProductSyncFlow < ActiveDataFlow::DataFlow
  def initialize(config = {})
    @source = ActiveRecord.new(
      model: Product,
      scope: ->(r) { r.where(active: true) }
    )
    @sink = ActiveRecord.new(model: ProductExport)
  end

  def transform(data)
    {
      product_id: data['id'],
      price_cents: (data['price'].to_f * 100).to_i,
      category_slug: data['category']&.parameterize,
      exported_at: Time.current
    }
  end
end</code></pre>
    </div>
  </div>

  <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 rounded-r-lg mb-6">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
        </svg>
      </div>
      <div class="ml-3">
        <p class="text-sm text-yellow-700">
          <strong class="font-medium">Development Status:</strong>
          The ActiveDataFlow gems are currently being implemented. 
          The DataFlow code is ready but temporarily inactive until the core framework is complete.
        </p>
      </div>
    </div>
  </div>

  <div>
    <%= link_to "View Full DataFlow Details →", active_data_flow_data_flows_path, class: "#{button_classes(:primary)} bg-purple-600 hover:bg-purple-700 focus:ring-purple-500" %>
  </div>
</div>

<!-- Quick Actions -->
<div class="text-center">
  <h3 class="text-2xl font-semibold text-gray-900 mb-6">Quick Actions</h3>
  <div class="flex flex-wrap justify-center gap-3">
    <%= link_to "📦 View Products", products_path, class: button_classes(:primary) %>
    <%= link_to "📄 View Exports", product_exports_path, class: button_classes(:success) %>
    <%= link_to "⚡ View DataFlow", active_data_flow_data_flows_path, class: "#{button_classes(:primary)} bg-purple-600 hover:bg-purple-700 focus:ring-purple-500" %>
    <%= button_to "💓 Trigger Heartbeat", heartbeat_click_path, method: :get, class: "#{button_classes(:primary)} bg-orange-600 hover:bg-orange-700 focus:ring-orange-500" %>
    <%= button_to "🔄 Reset Demo", reset_path, method: :delete, data: { turbo_confirm: "This will delete all product exports and data flow runs. Are you sure?" }, class: button_classes(:danger) %>
  </div>
</div>
